document.addEventListener("DOMContentLoaded",()=>{
const box=document.getElementById("profileBox"),u=currentUser();if(!u){box.innerHTML='<div class="detail-card"><h1>Please login</h1><p>Create an account to view your profile.</p><a class="btn btn-primary" href="login.html">Login</a></div>';return}
const ps=getProblems().filter(p=>p.author===u.name);box.innerHTML=`<div class="detail-card"><p class="eyebrow">Your account</p><h1>${esc(u.name)}</h1><p>${esc(u.email)}</p><div class="stats" style="margin:25px 0 0"><div><strong>${ps.length}</strong><span>Problems</span></div><div><strong>${ps.filter(p=>p.status==="SOLVED").length}</strong><span>Solved</span></div><div><strong>${ps.reduce((n,p)=>n+(p.solutions||0),0)}</strong><span>Solutions</span></div><div><strong>0</strong><span>Accepted</span></div></div><div style="margin-top:45px"><h2>Your problems</h2><div class="problem-grid">${ps.length?ps.map(card).join(""):'<p class="muted">You have not registered a problem yet.</p>'}</div></div><button id="logout" class="btn btn-secondary" style="margin-top:30px">Logout</button></div>`;document.getElementById("logout").onclick=()=>{localStorage.removeItem("ps_user");location.href="index.html"}
});
